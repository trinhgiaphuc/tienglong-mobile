import * as React from 'react';
import { Text, View } from 'react-native';

export default function AddWordScreen({navigation}) {
  return (
    <View className="h-full w-full bg-red-300">
      <Text className="text-2xl text-center">
        Hello 
      </Text>    
    </View>
  );
}

